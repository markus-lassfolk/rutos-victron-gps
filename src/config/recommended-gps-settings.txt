// Recommended GPS Settings for RV Use
// =====================================

// GPS Source Accuracy Requirements
rutos_accuracy: 8,              // Accept RUTOS if accuracy < 8m (realistic threshold)
starlink_accuracy: 15,          // Accept Starlink if accuracy < 15m (fallback)

// Movement Detection Precision  
position_eps: 1e-5,             // ~1 meter coordinate precision (reduces jitter)
altitude_threshold: 15,         // 15m altitude change (accounts for source differences)
speed_threshold: 1.4,           // 1.4 m/s = 5 km/h (walking speed, filters GPS drift)

// Smart Movement Thresholds
camping_threshold: 20,          // 20m = significant campsite movement (above GPS noise)
obstruction_threshold: 50,      // 50m = moved to new location (worth Starlink reset)
stationary_time: 900000,       // 15 minutes (900,000 ms) - realistic camping stops

// System Behavior
source_override: 'auto',        // Automatic source selection
log_level: 'info'              // Standard logging level

/* 
REASONING:
- camping_threshold: 20m is above your 8m GPS accuracy, prevents false positives
- obstruction_threshold: 50m ensures you've actually moved to a new spot
- speed_threshold: 1.4 m/s = 5 km/h filters out stationary GPS drift
- position_eps: 1e-5 degrees ≈ 1m reduces coordinate jitter while staying precise
*/
